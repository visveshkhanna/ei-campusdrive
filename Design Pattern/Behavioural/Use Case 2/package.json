{
  "name": "stock-investor-observer",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "npx ts-node src/index.ts",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "A Stock (Subject), Investor (Observer) model that uses the `Observer` pattern which monitors the Stock's price changes and notifies the Investors of the price changes."
}
